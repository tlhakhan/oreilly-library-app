# oreilly-library-app
Quickly find and explore books

## setup
- Run the `db/seed.js` file to populate the local cache database
- Download the book covers using the `db/books.json` generated by the `db/seed.js` and the script `public/cover/download_cover.sh`
